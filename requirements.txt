# Runtime dependencies for aghc-s128 (bounded, not strictly pinned —
# use environment.yml or pip freeze for exact reproduction snapshots).
numpy>=1.24,<3
scipy>=1.10,<2
matplotlib>=3.7,<4
PyYAML>=6.0,<7

# Testing
pytest>=7.0
